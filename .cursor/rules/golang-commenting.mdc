---
description: Go 代码注释统一规范（结构体/方法/常量/全局变量）
alwaysApply: true
---

# Go 注释规范（全局生效）

适用范围：所有新增或修改的 Go 代码文件。

- `struct` 必须有中文注释，说明结构体用途。
- `struct` 每个字段必须有中文注释，说明语义与约束（如是否敏感、单位、默认值）。
- 每个可导出方法必须有中文注释，并使用统一格式：首行概述方法用途；随后按需使用“输入：”“输出：”段落，分别说明关键参数与返回值语义（无输入/输出时明确写“无”）。
- 每个非导出方法必须有中文注释并遵循同一格式；简单方法可仅保留用途概述，复杂方法需补充“输入：”“输出：”说明。
- 关键逻辑必须加必要中文注释，说明“为什么这样做”，避免只写显而易见的“做了什么”。
- 常量必须有中文注释，说明业务语义或使用场景。
- 全局变量必须有中文注释，说明生命周期、初始化方式与作用。

## 风格要求

- 注释要简洁、准确、可维护，避免空话与重复代码字面含义。
- 敏感信息（密码、token、密钥等）不得写入注释、日志或错误明文。
- 与现有导出符号命名保持一致，避免注释与实现语义不一致。
- 在 `const`/`var` 块中，若多个常量/变量都包含单行注释，则每个常量/变量与下一个常量/变量之间必须用一个空行分隔，提升可读性。
- 在 `struct`/`interface` 定义中，若多个成员都包含单行注释，则每个成员与下一个成员之间必须用一个空行分隔，提升可读性。
