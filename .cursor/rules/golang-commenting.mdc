---
description: Go 代码注释统一规范（结构体/方法/常量/全局变量）
alwaysApply: true
---

# Go 注释规范（全局生效）

适用范围：所有新增或修改的 Go 代码文件。

- `struct` 必须有注释，说明结构体用途。
- `struct` 每个字段必须有注释，说明语义与约束（如是否敏感、单位、默认值）。
- 每个可导出方法必须有注释，至少包含：方法用途、输入参数、输出参数/返回值语义。
- 每个非导出方法必须有注释说明方法用途；复杂方法需补充主要参数说明。
- 关键逻辑必须加必要注释，说明“为什么这样做”，避免只写显而易见的“做了什么”。
- 常量必须有注释，说明业务语义或使用场景。
- 全局变量必须有注释，说明生命周期、初始化方式与作用。

## 风格要求

- 注释要简洁、准确、可维护，避免空话与重复代码字面含义。
- 敏感信息（密码、token、密钥等）不得写入注释、日志或错误明文。
- 与现有导出符号命名保持一致，避免注释与实现语义不一致。
