---
description: Dart/Flutter 代码注释统一规范（类/字段/方法/常量/全局变量）
globs: frontend/**/*.dart
alwaysApply: false
---

# Dart/Flutter 注释规范

适用范围：`frontend/**/*.dart` 的新增或修改代码。

- `class` 必须有注释，说明类职责与使用场景。
- 类字段必须有注释，说明语义、单位、默认值或约束。
- 公有方法必须有注释，至少包含：方法用途、输入参数、返回值/副作用说明。
- 私有方法必须有注释说明用途；复杂方法需补充主要参数与关键分支说明。
- 关键业务逻辑、状态转换、异步边界（如 `Future`/`Stream`）必须添加必要注释，说明“为什么这样做”。
- 常量必须有注释，说明业务语义与取值来源。
- 全局变量（含顶层变量、单例实例）必须有注释，说明生命周期与作用域。

## 风格要求

- 优先使用 Dart 文档注释：公有 API 使用 `///`，复杂实现可配合行内 `//` 注释。
- 注释应简洁、准确、可维护，避免复述代码字面含义。
- 敏感信息（密码、token、密钥等）不得写入注释、日志或错误明文。
- 注释语义必须与实现保持一致，变更代码时同步更新注释。
